# 参与开发

感谢你帮助验证或改进璞趣标签打印机的浏览器端适配。本项目是对厂商小程序 SDK 协议与传输行为的独立整理，不代表璞趣官方实现。提交兼容性结论时，请注明打印机完整型号、DPI、固件版本、浏览器版本和操作系统。

## 本地开发

要求 Node.js 20 或更高版本，并使用仓库内的 `package-lock.json` 安装依赖：

```bash
npm ci
npm run test:watch
```

提交前运行完整校验：

```bash
npm run validate
```

## 核心代码规范

项目使用严格模式 TypeScript，并按职责保持三层边界：

- `src/protocol.ts` 只处理字节协议、位图压缩、命令构造和通知解析，不访问浏览器 API；
- `src/web-bluetooth.ts` 只处理设备选择、GATT 服务/特征发现、分包、串行队列和连接状态；
- 框架组件、标签排版和业务数据留在应用层或 `examples/`，不进入核心 SDK。

新增或修改代码时遵守以下约定：

1. 公共 API 必须有明确的 TypeScript 类型；不能使用 `any` 绕过设备数据校验。
2. 协议常量使用大写命名，所有字节数组使用 `Uint8Array`；输入位图使用 RGBA，每像素 4 字节。
3. 协议函数应保持纯函数，不能读写全局状态；同一输入必须产生相同字节结果。
4. 所有 GATT 写入必须经过内部操作队列和分包逻辑，不能从公共方法并发直写特征值。
5. 可预期的连接、权限、设备和打印错误使用稳定的 `PuquPrinterError.code`；界面展示文案不能作为程序判断条件。
6. 新增机型 UUID 时必须有可追溯的实机或 SDK 依据，并保留通过 `additionalServiceUuids` 外部配置的能力。
7. 不提交业务标签、用户数据、设备序列号、厂商 SDK 原包或其他无再分发授权的文件。
8. 引用厂商 SDK、协议资料或其他第三方材料时，必须记录来源和适用许可证；无法确认授权时，请通过[璞趣官网联系页面](https://www.puqulabel.com/about/)向厂商取得书面许可后再提交。

## 测试规范

基础测试使用 Vitest，测试文件与源码放在同一目录并命名为 `*.test.ts`：

- 协议测试断言完整字节序列，至少覆盖 200/300 DPI、透明像素、非 8 倍数宽度和非法输入；
- 传输测试使用注入的 `PuquBluetooth` 模拟对象，不要求 CI 具备蓝牙硬件；
- 修复缺陷时先增加一个能够复现问题的测试；
- 自动测试不能替代实机测试。涉及 UUID、MTU、分包节奏、状态通知或新机型时，必须按 README 的实机清单复核。

## 提交建议

保持提交内容单一且可审查。协议行为发生变化时，在提交说明中附上输入、期望字节、来源依据和已验证机型；不要只描述页面上的视觉结果。
